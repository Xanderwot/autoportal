<div class="sign_in">
	<% if current_user.nil? %>
		<%= link_to t('sign_in'), new_user_session_path %>
		<%= t('or') %>
		<%= link_to t('sign_up'), new_user_registration_path %>
	<% else %>
		<%= t('signed_as') %>
		<%= current_user.email %>
		<%= link_to t('sign_out'), destroy_user_session_path, :method => :delete %>
	<% end %>
		<div class="logo_link">
			<%= link_to "Auto Portal", root_path %>
		</div>
</div>
